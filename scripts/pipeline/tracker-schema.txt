# The tracker workspace the pipeline needs. One definition: tracker.sh setup creates it (through the tracker's CLI),
# doctor.sh checks it (tracker.sh setup --check), and the test suite checks it against docs/pipeline/TICKETS.md.
# Format: <type>|<name>|<values, comma-separated>|<note>
#   label-group  a label group whose labels are mutually exclusive (Linear: a label group; Jira: a single-select field;
#                GitHub/GitLab: prefixed labels the adapter keeps to one)
#   labels       plain labels
#   status       <pipeline state>|<Linear workflow status> (other trackers: scripts/pipeline/tracker.map)
label-group|Stage|product,analysis,build,dev,qa,staging,go-live,production,done,on-hold|one at a time: where the parent ticket is
label-group|Owner|product-owner,business-analyst,engineer,devops,qa,app-specialist,owner|one at a time: who holds the parent ticket
labels|Kind|story,eng,defect,follow-up|on child tickets
status|open|Todo|
status|reopened|Todo|
status|in-progress|In Progress|
status|fixed|In Review|not in a default Linear team; add it as a Started-type status
status|verified|Done|
status|done|Done|
status|wontfix|Canceled|
